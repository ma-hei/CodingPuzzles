#include <iostream>

using namespace std;

int main() {
    int t;
    cin >> t;
    for (int t_ = 0; t_ < t; t_++) {
        int n, m, k;
        int* arr[n];
        for (int n_ = 0; n_ < n; n_++) {
            cin >> arr[n_];
        }
        
    }
}
