#include <iostream>

using namespace std;

int main(int argc, char** argv) {
    int t;
    cin >> t;
    for (int t_=0; t_<t; t_++) {
        int n, k, m;
        cin >> n >> k >> m;
        string s;
        cin >> s;
    }
}
